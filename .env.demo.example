# Demo overrides – copy to .env.demo. Used when you run: npm run dev:demo
# Vite loads .env first (dev keys + shared bucket names), then this file.
# Only set what demo needs; bucket names come from .env (shared).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_USE_FAKE_DATA=true
